// Safe defaults. Local and CI secrets may override these values.
POCO_BACKEND = mock
SUPABASE_URL =
SUPABASE_ANON_KEY =
POCO_DEVELOPMENT_USER_ID =
POCO_MEMBERSHIP_PRODUCT_ID = com.fumiakiMogi777.poco.member.monthly
POCO_MEMBERSHIP_SYNC_FUNCTION =
POCO_AD_PROVIDER = placeholder
POCO_AD_UNIT_ID =
// Temporary public host. Keep this host alive after adding a Poco-owned domain
// so QR codes already printed with the GitHub Pages URL continue to work.
POCO_PUBLIC_BASE_URL = https:/$()/ben-kei-create.github.io
POCO_ASSOCIATED_DOMAIN = ben-kei-create.github.io
CODE_SIGN_ENTITLEMENTS = poco/poco.entitlements

#include? "Secrets.xcconfig"
